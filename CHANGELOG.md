# Changelog

## Unreleased

- **발주처 양식 대응을 집필 단계로 끌어올림.** 지금까지 양식은 변환가에게만 전달돼, 서식이 어긋난 초안을 변환 단계에서 뒤늦게 맞추다 위계가 밀렸다. 이제 양식이 있으면 설계·집필·검수에도 전달한다.
  - **집필** — 「발주처 양식이 있으면 서식 규격을 먼저 뽑는다」 절 신설. 문서 구성 순서·장 골격·계층 체계·글머리 레벨·표/그림 캡션 위치·강조 관행·서식 수치를 집필 착수 전에 계약으로 정리하게 했다. **양식이 둘 이상이면 글머리 체계가 서로 달라**(같은 `○`·`-`가 양식마다 다른 레벨) 산출물 모드를 확정하고 한쪽만 적용하도록 못박았다.
  - **설계** — 발주처 양식 골격이 사용자 승인 목차보다 우선. 양식이 장 구조를 제출 요건으로 고정하되 장 제목 변경을 허용하는 경우가 흔하므로, 골격은 양식을 따르고 장 제목·절 구성만 논증 흐름으로 채운다. 목차 작성 단계(보통 '절'까지)도 양식을 따른다.
  - **검수** — **축 6 「발주처 양식 준수」 신설**(4축 → 6축). 글머리 체계 혼용과 마크다운 리스트 기호 사용을 [필수]로 걸었다. 표 제목은 위·그림 제목은 아래가 자주 뒤집히는 지점이라 별도 항목으로 뒀다.
  - **오케스트레이터** — 양식 라우팅 규칙 추가.
- **모델 티어 규약 명확화** — "스폰 시 `model`을 명시하지 않아야 티어 기본값이 적용된다"를 티어 표·에이전트 구성·Phase 2 세 곳에 명문화. 에이전트 정의에 티어를 넣어도 스폰에서 모델을 지정하면 덮어써져 무력화되는데, 이것이 실측에서 확인된 성능 병목의 핵심이었다.

- **실행 시간 재조정 — 안내 예산을 실측 기반으로 다시 씀.** 4쪽 브리프 1건을 끝까지 완주해 단계별 시간을 측정한 결과(총 약 110분), 기존 예산(표준 180분 / 브리프 90분)이 목표와 어긋나 있었다. 새 예산은 **표준 52분 / 브리프 23분**이며, 숫자만 줄이지 않고 시간을 먹던 구조를 함께 고쳤다:
  - **예산이 분할 병렬을 전제한다** — 조사 2~4분할, 표준 집필 장 배치 **병렬**(기존 순차), 표준 검수 **축 분할 병렬**. 실측상 조사를 순차로 돌리면 39분, 병렬이면 20분이었다. 예산 초과의 첫 원인은 대개 병렬화 누락이므로, 래더 2단계(다운시프트) 전에 1단계(분할·병렬화) 적용 여부를 먼저 확인하도록 명시.
  - **브리프 모드 경량화** — 설계게이트는 그대로 수행하되 **designer 개정 라운드 폐지**(지적은 리더가 집필 계약으로 직접 하달), **writer 수정 재스폰 폐지**(비창작성 [필수]는 리더가 직접 Edit). 실측에서 이 둘이 34분을 먹었다.
  - **조사 상한 신설** — 분할당 도구 호출 20회·출처 10건. 상한 없이 두면 4쪽 브리프에도 분할당 60회를 쓴다(실측). 상한 도달과 `[미확보]`를 구분해 기록하도록 규정.
  - **병렬 스폰의 항목 번호 계약** — 설계 개정과 조사를 병렬로 돌리면 조사 목록 번호가 어긋난다(실측 확인된 사고 지점). "번호는 바꾸지 말고 추가만"을 각 프롬프트에 넣도록 명시.
- **검수 규율 보강 — 자기검사망의 구조적 한계를 명문화.** 완주 검증에서 3회 반복 관측된 패턴: **리더가 준 금지·검증 목록이 곧 에이전트의 자기검사 범위가 된다.** 집필가 자기보고는 전 항목이 재현됐는데 [필수] 지적은 전부 목록 밖에서 나왔고, 변환가는 검증 스위트를 개수 기준으로만 확인해 결함본을 "완벽"으로 보고했다. 검수관 프롬프트에 "목록 밖 영역(표↔본문 정합, 각주·참조 무결성, 근거 셀과 논지의 크기 일치)을 별도로 훑어라"를 추가하고, 개정이 2차 결함(표 셀 수정 → 각주 고아화)을 만든다는 점과 리더의 전수 재현 검증을 상시 항목으로 규정.
- **docs: 강점 한눈에 · 연결 가이드 추가** (`docs/strengths.md`) — 강점 7개를 실측 수치와 함께 한 장으로 정리하고, 연구 성격별 MCP·API 연결 추천(법령·의안·통계·학술·해외·ODA·지자체)을 신설. README 국·영문에 링크.

## v1.1.0 (2026-08-10)

- **성능 폴백 장치 추가** — 무거운 세션 모델(opus/sonnet)로 전 단계가 돌아 오래 걸리는 문제 대응. 4겹 구조:
  - ① 단계별 모델 티어 기본값: 조사 `sonnet`·변환 `haiku`, 설계·집필·검수는 세션 상속 유지
  - ② 쾌속 프로파일("빨리" 요청 시): 조사 분할 병렬, 집필 장 배치 분할(체크포인트), 수정 루프 2→1회, 출처 검증은 핵심 전수+주변 표본(미개봉 출처 명시)
  - ③ 지연 폴백 래더: `_workspace/_run_log.md`에 단계별 시각 기록, 안내 예산 초과 시 분할·병렬화 → 다운시프트 → 루프 축소 → 사용자 범위 협상 순 적용
  - ④ 실패 시 업시프트: 하위 모델 단계가 자체 검증에 실패하면 한 단계 위 모델로 1회 재스폰
  - 검증 게이트(reviewer)는 어떤 프로파일·래더 단계에서도 경량화하지 않는다.
- **라이선스 Apache-2.0 → MIT.** 이 저장소의 내용은 프롬프트·마크다운 자산이라 Apache-2.0의 핵심 이점인 특허 실시권 조항에 실익이 없고, §4(b)의 "변경 사실 고지" 의무가 이 저장소의 의도된 사용(각자 분야·기관에 맞게 고쳐 쓰기)과 오히려 충돌한다. Claude Code 스킬·플러그인 생태계의 사실상 표준도 MIT다. MIT는 Apache-2.0보다 제약이 적으므로 기존 이용자의 권리는 축소되지 않는다.
- README를 국문·영문 2종으로 전면 개편 — 제작 배경과 활용 시나리오 보강 (`README.md` / `README.en.md`)

## v1.0.0 (2026-07-20)

- **표준 보고서 모드(50p급) 전 과정 예제 추가** — `examples/policy-standard-demo/`: 같은 주제(고령자 디지털 정보격차)의 8장·본문 65p급·표 11종·참고문헌 51건 완주 기록.
  - 게이트1이 [필수] 4건(RQ↔조사 불일치·병목 판정 기준 부재·연쇄 규칙 부재·수용성 근거 전무)을 적발 → 설계 v2 반영
  - 게이트2 표본 대조 40건 불일치 0건, 참고문헌 51건 유령·누락 0건, [미확보] 5건 한계 절에 정직 기재
  - hwpx 정량 검증: 표 69/69 행 전수 일치, 본문 왕복 유사도 1.000000, 약 65p
- 브리프 모드 예제(4쪽)와 같은 주제라 **두 산출 모드의 규모·게이트 강도 대비**를 한눈에 볼 수 있다.

## v0.9.0 (2026-07-20) — 공개 후보(Release Candidate)

- 통합 저장소(policy-research-kits)에서 **단독 저장소로 분리** — 이 저장소 하나로 마켓플레이스 등록·설치 가능.
- 전 과정 예제(examples/)·검증 게이트 방법론(docs/) 동봉.
- 공개판 조정: `model: inherit` 기본, 팀 API 부재 시 순차 Agent 폴백, BYO-template, geo-search 내장.

다른 킷: https://github.com/parkjui92/rnd-proposal-kit · https://github.com/parkjui92/socsci-paper-kit
